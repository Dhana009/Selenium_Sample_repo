[pytest]
markers=
        homepage: marks tests as homepage tests

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

#addopts = -v -s --html=report.html --self-contained-html -n auto  #runs the test cases in verbose mode and shows console logs